defmodule Meadow.Data.Posters do
  # alias Meadow.Data.FileSets

  require Logger

  def create_with_offset(id, offset) do
    Logger.info("WE'RE PRETENDING TO CREATE A POSTER NOW FOR #{id} WITH OFFSET #{offset}!")
  end
end
